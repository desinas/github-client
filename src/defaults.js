//the api that will be used for data fetch
const API = 'https://api.github.com';

//the number of users in a list page
const USER_PER_PG = 8;

//the number of repos in a list page
const REPO_PER_PG = 6;

export { API, USER_PER_PG, REPO_PER_PG };